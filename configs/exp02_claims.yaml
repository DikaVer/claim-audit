# Stage 02: extract atomic claims from the agent's visible messages and final report.
extractor_model: null     # null means use models.yaml -> extractor.deployment. Set a deployment name here to override for one run.
claim_schema_version: 3   # must equal CLAIM_SCHEMA_VERSION in schema.py, which versions the whole inter-stage contract
